db:validate:
  stage: test
  image: docker:27
  services:
    - docker:27-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  script:
    - docker compose -f db/compose.yml config
  rules:
    - changes:
        - db/**/*
